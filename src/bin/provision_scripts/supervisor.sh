#!/usr/bin/env bash

# Install supervisor
sudo apt-get install Supervisor

# Create configuration file for webapp inside. /etc/supervisor/conf.d/*.conf

# Run supervisor as a user which is running gunicorn.
