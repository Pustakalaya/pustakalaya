writing model to doctype
=========================
The common field that shares all the fields for the doctype is written inside `core/search.py` packege with the
class name of an `Item`

- All Document, Audio, Video, Image, Maps, Wiki, inherint this Doctype

