Core application
------------------

Core app contains most of the logic that are sharable across other apps. For instance
`TimeStampModel` is an `Abstract` class used by all others application to create
